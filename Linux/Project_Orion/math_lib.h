#pragma once
#include <math.h>
namespace Project_Orion
{
	float latitudecal();
	float longitudecal();
	float yAxisProjection(float input);
	float xAxisProjection(float input);
	size_t randomColor();
}
